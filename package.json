{
  "name": "whydoughcookies",
  "version": "1.0.0",
  "private": true,
  "description": "Why Dough Cookies — static site + Cloudflare Pages Functions",
  "scripts": {
    "dev": "wrangler pages dev . --port 8788",
    "deploy": "wrangler pages deploy --project-name whydoughcookies",
    "deploy:prod": "wrangler pages deploy --project-name whydoughcookies --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
